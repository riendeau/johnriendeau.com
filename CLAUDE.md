# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

This is a **GitHub Pages configuration repository** for the custom domain `johnriendeau.com`. It contains no application code or build system — GitHub Pages serves the content automatically from the `main` branch.

## Repository Contents

- **CNAME** — Custom domain configuration for GitHub Pages (points to `johnriendeau.com`)
- **ads.txt** — Google AdSense ad authorization declaration

## Development

There are no build, test, or lint commands. Changes are deployed automatically by GitHub Pages when pushed to `main`.
